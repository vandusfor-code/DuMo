import "server-only";
import type { Expense } from "@/types/accounting";

export const EXPENSES_MOCK: Expense[] = [];

export const ACCOUNTING_MONTHLY_BUDGET = 0;

export const ACCOUNTING_CHART_MOCK: { label: string; income: number; expenses: number; profit: number }[] = [];

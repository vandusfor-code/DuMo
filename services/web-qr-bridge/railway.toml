# Railway: crear servicio con Root Directory = services/web-qr-bridge
# Montar volumen persistente en /data/sessions y definir SESSIONS_DIR=/data/sessions

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
